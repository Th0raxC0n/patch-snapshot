{
  "name": "patch-snapshot",
  "version": "0.2.0",
  "description": "Evidence-first EVM contract permission snapshots",
  "type": "module",
  "bin": {
    "patch-snapshot": "./src/index.mjs",
    "patch-payment": "./src/payment.mjs"
  },
  "scripts": {
    "build:standalone": "node ./scripts/build-standalone.mjs",
    "pretest": "node ./scripts/build-standalone.mjs",
    "test": "node --test",
    "snapshot:base": "node ./src/index.mjs",
    "verify:payment": "node ./src/payment.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
